# Script needed by //third_party/bazel:maven_jar.bzl. This build target isn't
# required, it is just used here to document the implicit dependency.
filegroup(
    name = "maven_scripts",
    srcs = [
        "download_file.py",
        "util.py",
    ],
)
